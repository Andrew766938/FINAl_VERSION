# 🚀 РУКОВОДСТВО ПО УСТАНОВКЕ И ЗАПУСКУ

## 📋 Требования

- Python 3.12+
- pip или uv package manager
- Git

## 🔧 Пошаговая установка

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Andrew766938/FINAL_VERSION.git
cd FINAL_VERSION
```

### Шаг 2: Создание виртуального окружения

**Вариант A: Встроенный venv**
```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# Linux/Mac
source .venv/bin/activate
```

**Вариант B: uv**
```bash
uv venv
source .venv/bin/activate  # или .venv\Scripts\activate на Windows
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

или с uv:
```bash
uv sync
```

### Шаг 4: Конфигурация окружения

1. Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

2. Отредактируйте файл `.env` и установите переменные:

```env
SECRET_KEY=your-super-secret-key-at-least-32-characters-long-12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
DB_NAME=blog.db
```

**Важные замечания:**
- `SECRET_KEY` должен быть длинной случайной строкой в production
- Можно сгенерировать с помощью: `python -c "import secrets; print(secrets.token_urlsafe(32))"`

### Шаг 5: Запуск приложения

```bash
uvicorn main:app --reload
```

Вы должны увидеть:
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

## 🌐 Доступ к приложению

После запуска откройте в браузере:

| URL | Описание |
|-----|---------|
| http://127.0.0.1:8000/ | **Веб-интерфейс блога** |
| http://127.0.0.1:8000/docs | **Swagger API документация** |
| http://127.0.0.1:8000/redoc | **ReDoc API документация** |

## 📝 Первые шаги

1. **Откройте веб-интерфейс**: http://127.0.0.1:8000/

2. **Зарегистрируйтесь**:
   - Введите имя, email, пароль
   - Нажмите "Регистрация"

3. **Войдите в систему**:
   - Используйте email и пароль
   - Получите JWT токен

4. **Создайте первый пост**:
   - Введите заголовок и текст
   - Нажмите "Создать пост"

5. **Попробуйте функции**:
   - 👍 Лайкните пост
   - 💬 Добавьте комментарий
   - 👥 Добавьте друга (введите ID другого пользователя)

## 🐛 Решение проблем

### Ошибка: "ValidationError: 4 validation errors for Settings"

**Причина**: Отсутствует файл `.env`

**Решение**:
```bash
cp .env.example .env
# Отредактируйте .env и заполните все поля
```

### Ошибка: "ModuleNotFoundError: No module named 'app'"

**Причина**: Зависимости не установлены

**Решение**:
```bash
pip install -r requirements.txt
```

### Ошибка: "Port 8000 is already in use"

**Решение**: Используйте другой порт:
```bash
uvicorn main:app --reload --port 8001
```

### БД не создается

**Решение**: БД создается автоматически при первом запуске. Если нужно пересоздать:
```bash
rm blog.db  # (или используйте ваше DB_NAME из .env)
# Затем перезапустите приложение
```

## 🧪 Тестирование API

### Через Swagger (рекомендуется)

1. Откройте http://127.0.0.1:8000/docs
2. Кликните "Try it out" на нужном endpoint
3. Заполните параметры
4. Нажмите "Execute"

### Через curl

**Регистрация**:
```bash
curl -X POST "http://127.0.0.1:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "John Doe",
    "email": "john@example.com",
    "password": "securepassword123"
  }'
```

**Логин**:
```bash
curl -X POST "http://127.0.0.1:8000/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=john@example.com&password=securepassword123"
```

Ответ будет содержать `access_token`:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

**Создание поста** (используйте токен из логина):
```bash
curl -X POST "http://127.0.0.1:8000/posts/" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My First Post",
    "content": "This is an amazing blog post!"
  }'
```

## 📚 Структура файлов

```
FINAL_VERSION/
├── app/
│   ├── api/              # API маршруты
│   ├── models/           # ORM модели
│   ├── schemes/          # Pydantic DTO
│   ├── services/         # Бизнес-логика
│   ├── repositories/     # Доступ к данным
│   ├── exceptions/       # Кастомные ошибки
│   ├── frontend/         # Веб-интерфейс (HTML/JS)
│   ├── database/         # Конфигурация БД
│   └── config.py         # Настройки приложения
├── migrations/           # Alembic миграции БД
├── main.py              # Точка входа приложения
├── requirements.txt     # Зависимости Python
├── .env.example         # Шаблон переменных окружения
├── .env                 # Переменные окружения (не в git)
├── .gitignore           # Git исключения
├── alembic.ini          # Конфигурация Alembic
└── README.md            # Документация

```

## 🔐 Production развертывание

Перед развертыванием на production:

1. **Измените SECRET_KEY**:
   ```bash
   python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
   ```

2. **Используйте production ASGI сервер** вместо --reload:
   ```bash
   gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker
   ```

3. **Включите HTTPS** (используйте nginx или другой reverse proxy)

4. **Установите переменную окружения**:
   ```bash
   export PYTHONUNBUFFERED=1
   ```

## ❓ Часто задаваемые вопросы

**Q: Как сбросить БД?**
A: Удалите файл `blog.db` (или используйте ваше DB_NAME) и перезапустите приложение.

**Q: Как создать пользователя для тестирования?**
A: Используйте веб-интерфейс или API endpoint `/auth/register`.

**Q: Как изменить порт?**
A: Запустите: `uvicorn main:app --reload --port 8001`

**Q: Где находятся логи?**
A: Логи выводятся в консоль. Для файловых логов нужно добавить логирование в код.

## 📞 Поддержка

Если вы столкнулись с проблемами:

1. Проверьте, что все зависимости установлены: `pip list | grep -i fastapi`
2. Убедитесь, что файл `.env` существует и заполнен
3. Проверьте версию Python: `python --version` (должна быть 3.12+)
4. Откройте issue на GitHub с описанием проблемы

## 📖 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [Pydantic документация](https://docs.pydantic.dev/)
- [JWT аутентификация](https://tools.ietf.org/html/rfc7519)

---

**Удачи с проектом!** 🎉
