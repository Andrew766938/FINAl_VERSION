# 👁️ Гостевой режим Betony

## 🎯 Описание

Гостевой режим позволяет пользователям просматривать содержимое платформы **без регистрации**.

## ✅ Возможности гостя

- 📝 **Просмотр постов** — можно читать все публикации
- 💬 **Просмотр комментариев** — чтение обсуждений под постами
- 👥 **Просмотр пользователей** — список зарегистрированных пользователей
- ❤️ **Просмотр лайков** — счётчики лайков на постах

## ❌ Ограничения гостя

Гость **НЕ МОЖЕТ**:

- ❌ Создавать посты
- ❌ Комментировать
- ❌ Ставить лайки
- ❌ Добавлять друзей
- ❌ Иметь профиль
- ❌ Иметь избранные посты

## 🚀 Как использовать

### 1. На странице входа

Нажмите кнопку **"Войти как гость"** под формой логина.

### 2. В гостевом режиме

- Вы увидите **"Гость 👁️"** в верхней панели
- Форма создания постов будет скрыта
- Все кнопки действий будут заблокированы с иконкой 🔒
- Вкладки "Избранное", "Друзья" и "Профиль" будут скрыты

### 3. Выход из гостевого режима

Нажмите **"Выход"** в верхней панели, чтобы вернуться к странице входа.

## 🛠️ Технические детали

### Файлы

- `app/frontend/guest.js` — JavaScript логика гостевого режима
- `app/frontend/guest.css` — стили для гостевого режима

### Объект гостя

```javascript
currentUser = {
  id: -1,
  username: 'Гость',
  email: 'guest@local',
  is_admin: false,
  is_guest: true  // Флаг гостя
};
```

### Функции

- `loginAsGuest()` — вход в гостевой режим
- `showAppAsGuest()` — отображение приложения для гостя
- `loadPostsReadOnly()` — загрузка постов в режиме только для чтения
- `renderPostReadOnly()` — отрисовка поста без интерактивных элементов
- `loadUsersReadOnly()` — загрузка списка пользователей

## 🌐 API

Гостевой режим использует **публичные эндпоинты** без авторизации:

- `GET /posts/` — получение всех постов
- `GET /posts/{id}/comments` — получение комментариев
- `GET /posts/{id}/likes` — получение лайков
- `GET /auth/users` — получение списка пользователей

Никакие `Authorization` заголовки не отправляются.

## 💡 Преимущества

1. **Низкий порог входа** — новые пользователи могут познакомиться с платформой
2. **Безопасность** — нет доступа к функциям изменения данных
3. **SEO-френдли** — контент доступен для индексации поисковыми системами
4. **Простота** — один клик для доступа к контенту

## 🔐 Безопасность

- Гость не имеет `authToken`
- Все POST/PUT/DELETE запросы недоступны
- ID гостя `-1` не существует в БД
- Все формы и кнопки действий заблокированы на уровне UI

---

**Сделано с ❤️ для Betony**
