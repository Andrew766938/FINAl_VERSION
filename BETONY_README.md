# 🌿 Betony - Social Blog Platform

## Обзор

**Betony** - это современная социальная платформа для блогинга с полнофункциональным интерфейсом и REST API.

### Основные возможности:
- ✅ Аутентификация (регистрация/логин)
- ✅ Создание и удаление постов
- ✅ Комментирование постов
- ✅ Система лайков
- ✅ Добавление друзей
- ✅ Просмотр профилей
- ✅ Многовкладочный интерфейс
- ✅ Красивый, современный дизайн

---

## 📋 Структура проекта

```
FINAL_VERSION/
├── app/
│   ├── api/                 # API endpoints
│   │   ├── auth.py         # Аутентификация
│   │   ├── posts.py        # Посты, комментарии, лайки
│   │   ├── friendships.py  # Дружба
│   │   ├── roles.py        # Роли
│   │   ├── likes.py        # Лайки
│   │   ├── comments.py     # Комментарии
│   │   └── dependencies.py # Зависимости
│   ├── frontend/
│   │   └── index.html      # Главная страница (SPA)
│   ├── models/             # SQLAlchemy модели
│   ├── services/           # Бизнес-логика
│   ├── schemes/            # Pydantic схемы
│   ├── database/           # DB конфиг
│   └── config.py           # Конфигурация
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости
└── test.db                 # SQLite БД
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

### 3. Открыть в браузере

```
http://localhost:8000
```

---

## 🔐 Аутентификация

### Регистрация

**Endpoint:** `POST /auth/register`

```json
{
  "username": "john_doe",
  "email": "john@example.com",
  "password": "secure_password"
}
```

**Ответ:**
```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "user": {
    "id": 1,
    "username": "john_doe",
    "email": "john@example.com"
  }
}
```

### Логин

**Endpoint:** `POST /auth/login`

```json
{
  "email": "john@example.com",
  "password": "secure_password"
}
```

### Получить всех пользователей

**Endpoint:** `GET /auth/users`

**Ответ:**
```json
[
  {
    "id": 1,
    "username": "john_doe",
    "email": "john@example.com"
  }
]
```

---

## 📝 Посты

### Создать пост

**Endpoint:** `POST /posts/`

**Headers:** `Authorization: Bearer {token}`

```json
{
  "title": "Мой первый пост",
  "content": "Это содержание поста..."
}
```

### Получить все посты

**Endpoint:** `GET /posts/`

**Query Parameters:**
- `skip` (int): пропустить N записей
- `limit` (int): количество записей
- `user_id` (int): только посты конкретного пользователя

### Удалить пост

**Endpoint:** `DELETE /posts/{post_id}`

**Headers:** `Authorization: Bearer {token}`

---

## 💬 Комментарии

### Получить комментарии поста

**Endpoint:** `GET /posts/{post_id}/comments`

**Ответ:**
```json
[
  {
    "id": 1,
    "post_id": 1,
    "user_id": 1,
    "content": "Отличный пост!",
    "created_at": "2025-12-16T12:00:00",
    "author_username": "john_doe"
  }
]
```

### Добавить комментарий

**Endpoint:** `POST /posts/{post_id}/comments`

**Headers:** `Authorization: Bearer {token}`

```json
{
  "content": "Отличный пост!"
}
```

---

## ❤️ Лайки

### Лайкнуть пост

**Endpoint:** `POST /posts/{post_id}/like`

**Headers:** `Authorization: Bearer {token}`

```json
{
  "user_id": 1
}
```

---

## 👥 Дружба

### Добавить друга

**Endpoint:** `POST /friendships/`

**Headers:** `Authorization: Bearer {token}`

```json
{
  "user_id": 1,
  "friend_id": 2
}
```

### Получить друзей пользователя

**Endpoint:** `GET /friendships/user/{user_id}`

**Ответ:**
```json
[
  {
    "id": 1,
    "user_id": 1,
    "friend_id": 2,
    "user": {
      "id": 1,
      "username": "john_doe",
      "email": "john@example.com"
    },
    "friend": {
      "id": 2,
      "username": "jane_doe",
      "email": "jane@example.com"
    }
  }
]
```

### Удалить друга

**Endpoint:** `DELETE /friendships/{friendship_id}`

**Headers:** `Authorization: Bearer {token}`

---

## 🎨 Фронтенд

### Страницы и вкладки

#### 1. **Лента** 📰
- Просмотр всех постов
- Создание новых постов
- Комментирование
- Лайкинг

#### 2. **Люди** 👥
- Список всех пользователей
- Добавление в друзья

#### 3. **Друзья** ❤️
- Ваши друзья
- Удаление из друзей

#### 4. **Профиль** 👤
- Информация о вас
- Статистика:
  - Количество постов
  - Количество друзей
  - Количество лайков

---

## 🔌 API Documentation

Полная документация API доступна на:
```
http://localhost:8000/docs
```

Или через ReDoc:
```
http://localhost:8000/redoc
```

---

## 🛠️ Технологический стек

**Backend:**
- FastAPI
- SQLAlchemy
- SQLite
- Pydantic
- Uvicorn

**Frontend:**
- HTML5
- CSS3
- JavaScript (Vanilla)
- Inter Font

---

## 📦 Требования

```
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
alembic>=1.12.0
```

---

## 🐛 Troubleshooting

### Ошибка подключения к API

1. Убедитесь, что сервер запущен на `http://localhost:8000`
2. Проверьте CORS настройки в `main.py`
3. Очистите кеш браузера

### БД не инициализирована

```bash
python -c "from app.database.connection import init_db; asyncio.run(init_db())"
```

### Ошибки аутентификации

- Убедитесь, что используете правильный email и пароль
- Проверьте, что токен передается в заголовке `Authorization: Bearer {token}`

---

## 📝 Лицензия

MIT License

---

## 👨‍💻 Разработка

### Ваш репозиторий
https://github.com/Andrew766938/FINAl_VERSION

### Последние обновления

✅ Полный редизайн фронтенда
✅ Добавлена система аутентификации с JWT
✅ Интегрированы все API endpoints
✅ Красивый многовкладочный интерфейс
✅ Логотип и брендинг Betony
✅ Система уведомлений об ошибках и успехе
✅ Адаптивный дизайн

---

## 🎯 Следующие шаги

- [ ] Добавить поиск по постам
- [ ] Категории для постов
- [ ] Система уведомлений
- [ ] Загрузка аватаров
- [ ] Темный режим
- [ ] Развертывание на сервер (Heroku, Railway)

---

**Спасибо за использование Betony! 🌿**
