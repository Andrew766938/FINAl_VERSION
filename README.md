# 📝 Блог - Полнофункциональное веб-приложение

Это полнофункциональное веб-приложение для создания и управления блогами с функциями регистрации, аутентификации, создания постов, комментариев, лайков и системой друзей.

## ✨ Возможности

- 🔐 **Регистрация и аутентификация** - безопасный вход с JWT токенами
- ✍️ **Управление постами** - создание, редактирование, удаление постов
- 💬 **Комментарии** - добавление и просмотр комментариев под постами
- 👍 **Лайки** - возможность лайкить посты
- 👥 **Система друзей** - добавление и удаление друзей
- 🎨 **Интерактивный интерфейс** - красивый и удобный веб-интерфейс
- 📱 **REST API** - полностью документированный API

## 🏗️ Архитектура

Проект следует чистой архитектуре с разделением на слои:

```
app/
├── api/                  # API маршруты (endpoints)
│   ├── auth.py          # Аутентификация
│   ├── posts.py         # Посты
│   ├── comments.py      # Комментарии
│   ├── likes.py         # Лайки
│   ├── friendships.py   # Друзья
│   └── dependencies.py  # Зависимости
├── models/              # ORM модели (SQLAlchemy)
│   ├── users.py
│   ├── posts.py
│   ├── comments.py
│   ├── likes.py
│   ├── friendships.py
│   └── roles.py
├── schemes/             # Pydantic DTO схемы
│   ├── posts.py
│   ├── comments.py
│   └── friendships.py
├── services/            # Бизнес-логика
│   ├── posts.py
│   ├── comments.py
│   ├── likes.py
│   └── friendships.py
├── repositories/        # Работа с БД (DAL)
│   ├── posts.py
│   ├── comments.py
│   ├── likes.py
│   └── friendships.py
├── exceptions/          # Кастомные исключения
│   └── exceptions.py
├── frontend/            # Веб-интерфейс
│   └── index.html       # Главная страница
└── database/            # Конфигурация БД
    └── database.py
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или с использованием uv:

```bash
uv sync
```

### 2. Настройка окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Затем отредактируйте `.env` и заполните следующие переменные:

```env
# JWT Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Configuration
DB_NAME=blog.db
```

**Важно!** Измените `SECRET_KEY` на что-нибудь безопасное в production!

### 3. Применение миграций (опционально)

Миграции БД применяются автоматически при первом запуске. Если нужно применить вручную:

```bash
alembic upgrade head
```

### 4. Запуск приложения

```bash
uvicorn main:app --reload
```

Приложение будет доступно на:
- **Web интерфейс**: http://127.0.0.1:8000/
- **API Swagger**: http://127.0.0.1:8000/docs
- **API ReDoc**: http://127.0.0.1:8000/redoc

## 📚 API Endpoints

### Аутентификация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Логин

### Посты
- `POST /posts/` - Создать пост
- `GET /posts/` - Получить все посты
- `GET /posts/{post_id}` - Получить пост по ID
- `GET /posts/user/{user_id}` - Получить посты пользователя
- `PUT /posts/{post_id}` - Обновить пост
- `DELETE /posts/{post_id}` - Удалить пост

### Комментарии
- `POST /comments/posts/{post_id}` - Добавить комментарий
- `GET /comments/posts/{post_id}` - Получить комментарии поста
- `GET /comments/{comment_id}` - Получить комментарий по ID
- `PUT /comments/{comment_id}` - Обновить комментарий
- `DELETE /comments/{comment_id}` - Удалить комментарий

### Лайки
- `POST /likes/posts/{post_id}` - Лайкнуть пост
- `DELETE /likes/posts/{post_id}` - Убрать лайк
- `GET /likes/posts/{post_id}` - Получить лайки поста
- `GET /likes/users/{user_id}` - Получить лайки пользователя

### Друзья
- `POST /friends/{friend_id}` - Добавить друга
- `DELETE /friends/{friend_id}` - Удалить друга
- `GET /friends/me` - Получить своих друзей
- `GET /friends/followers` - Получить подписчиков
- `GET /friends/{friend_id}/is-friend` - Проверить дружбу

## 🧪 Тестирование

Для тестирования API используйте встроенный Swagger:
1. Откройте http://127.0.0.1:8000/docs
2. Нажмите "Try it out" на нужном endpoint
3. Заполните параметры и нажмите "Execute"

Или используйте curl:

```bash
# Регистрация
curl -X POST "http://127.0.0.1:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"name":"John","email":"john@example.com","password":"password123"}'

# Логин
curl -X POST "http://127.0.0.1:8000/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=john@example.com&password=password123"

# Создать пост
curl -X POST "http://127.0.0.1:8000/posts/" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"My First Post","content":"Hello World!"}'
```

## 📦 Структура БД

### Таблица users
- id (INT, PK)
- name (VARCHAR)
- email (VARCHAR, UNIQUE)
- hashed_password (VARCHAR)
- role_id (INT, FK -> roles.id)

### Таблица posts
- id (INT, PK)
- title (VARCHAR)
- content (TEXT)
- user_id (INT, FK -> users.id)
- created_at (DATETIME)
- updated_at (DATETIME)
- likes_count (INT)

### Таблица comments
- id (INT, PK)
- content (TEXT)
- user_id (INT, FK -> users.id)
- post_id (INT, FK -> posts.id)
- created_at (DATETIME)
- updated_at (DATETIME)

### Таблица likes
- id (INT, PK)
- user_id (INT, FK -> users.id)
- post_id (INT, FK -> posts.id)
- created_at (DATETIME)

### Таблица friendships
- id (INT, PK)
- user_id (INT, FK -> users.id)
- friend_id (INT, FK -> users.id)
- created_at (DATETIME)

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- CORS поддержка
- Валидация входных данных через Pydantic

## 📝 Лицензия

MIT License

## 👤 Автор

AI Assistant Blog Project

## 🤝 Контрибьютинг

Приветствуются pull requests и issues!
