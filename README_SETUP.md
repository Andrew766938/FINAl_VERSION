# 🌿 Betony - Social Blog Platform

## 🚀 Быстрый Старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/Andrew766938/FINAl_VERSION.git
cd FINAl_VERSION
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Запуск сервера

**Вариант 1: Через Python напрямую**
```bash
python main.py
```

**Вариант 2: Через Uvicorn (рекомендуется)**
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 4. Открыть в браузере
```
http://localhost:8000
```

---

## 📋 Функциональность

✅ **Аутентификация**
- Регистрация
- Логин
- Выход

✅ **Посты**
- Создание
- Редактирование
- Удаление
- Просмотр ленты

✅ **Комментарии**
- Добавление комментариев
- Просмотр комментариев

✅ **Лайки**
- Добавление лайков
- Удаление лайков

✅ **Друзья**
- Добавление в друзья
- Удаление из друзей
- Просмотр списка друзей

✅ **Профиль**
- Просмотр профиля
- Статистика (посты, друзья, лайки)

---

## 🔌 API Endpoints

### Auth (Аутентификация)
```
POST   /auth/register       - Регистрация
POST   /auth/login          - Логин
GET    /auth/me             - Текущий пользователь
GET    /auth/users          - Все пользователи
GET    /auth/users/{user_id} - Конкретный пользователь
POST   /auth/logout         - Выход
```

### Posts (Посты)
```
GET    /posts/              - Все посты
POST   /posts/              - Создать пост
GET    /posts/{post_id}     - Конкретный пост
GET    /posts/user/{user_id} - Посты пользователя
PUT    /posts/{post_id}     - Обновить пост
DELETE /posts/{post_id}     - Удалить пост
```

### Comments (Комментарии)
```
GET    /posts/{post_id}/comments      - Комментарии поста
POST   /posts/{post_id}/comments      - Добавить комментарий
```

### Likes (Лайки)
```
POST   /posts/{post_id}/like    - Лайк
DELETE /posts/{post_id}/like    - Убрать лайк
```

### Friendships (Друзья)
```
GET    /friendships/user/{user_id}    - Друзья пользователя
POST   /friendships/                   - Добавить друга
DELETE /friendships/{friendship_id}    - Удалить друга
```

---

## 🐛 Решение Проблем

### Ошибка: "Module not found"
**Решение:**
```bash
pip install -r requirements.txt
```

### Ошибка: "Address already in use"
**Решение:** Порт 8000 уже занят
```bash
# Используй другой порт
uvicorn main:app --reload --port 3000
```

### Ошибка: "Database connection failed"
**Решение:** БД создается автоматически при первом запуске. Если ошибка повторяется:
```bash
# Удали старую БД и запусти заново
rm betony.db
python main.py
```

### Ошибка в браузере при регистрации/логине
**Решение:**
1. Открой DevTools (F12)
2. Проверь Console на ошибки
3. Убедись что сервер запущен на http://localhost:8000
4. Попробуй обновить страницу (Ctrl+F5)

---

## 📁 Структура Проекта

```
FINAL_VERSION/
├── main.py                 # Главный файл приложения
├── requirements.txt         # Зависимости
├── .env                     # Конфигурация
├── app/
│   ├── frontend/
│   │   └── index.html       # Фронтенд приложение
│   ├── api/
│   │   ├── auth.py          # Аутентификация
│   │   ├── posts.py         # Посты
│   │   ├── comments.py      # Комментарии
│   │   ├── likes.py         # Лайки
│   │   ├── friendships.py   # Друзья
│   │   └── dependencies.py  # Зависимости
│   ├── services/            # Бизнес-логика
│   ├── models/              # Модели БД
│   └── schemes/             # Схемы данных
└── betony.db                # База данных (создается автоматически)
```

---

## 🔐 Технологии

- **Backend:** FastAPI, Uvicorn
- **Database:** SQLAlchemy, SQLite
- **Frontend:** HTML5, CSS3, JavaScript
- **Auth:** JWT (JSON Web Tokens)

---

## 💡 Как Использовать

1. **Открой приложение:** http://localhost:8000
2. **Зарегистрируйся** или **войди**
3. **Создавай посты** в ленте
4. **Добавляй друзей** из раздела "Люди"
5. **Комментируй и лайкай** посты
6. **Просматривай профиль** с статистикой

---

## 📝 Примеры Запросов

### Регистрация
```bash
curl -X POST "http://localhost:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"name":"John","email":"john@example.com","password":"pass123"}'
```

### Создание поста
```bash
curl -X POST "http://localhost:8000/posts/" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"My Post","content":"Hello World","user_id":1}'
```

---

## 🎯 Что Дальше?

- Добавить уведомления
- Добавить поиск
- Добавить фильтры
- Добавить хэштеги
- Добавить медиа (фото, видео)
- Добавить мобильное приложение

---

**Готово к использованию! 🚀**
